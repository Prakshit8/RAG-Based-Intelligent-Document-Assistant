version: '3.8'

services:
  rag-assistant:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - INNGEST_EVENT_KEY=${INNGEST_EVENT_KEY}
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    
volumes:
  uploads:
